{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "ConnectionStrings": {
    // Old connection (SQL auth) — kept for reference
    //"DefaultConnection": "Server=192.168.1.111;Database=DBJuJuBi;User Id=UserConnection;Password=123;TrustServerCertificate=True;"

    // Windows Authentication (no username/password)
    "DefaultConnection": "Server=DESKTOP-1ULGF16\\JUJUBO;Database=DBJuJuBi;Trusted_Connection=True;TrustServerCertificate=True;"
  }

  // Jwt settings intentionally NOT here: the dev values live in
  // appsettings.Development.json; production must supply them via
  // environment variables (Jwt__Key, Jwt__Issuer, Jwt__Audience,
  // Jwt__ExpireMinutes) so the signing secret never sits in git.
}
